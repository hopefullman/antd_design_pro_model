#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_umi@2.6.13@umi/bin/umi.js" "$@"
  ret=$?
else 
  node  "$basedir/../_umi@2.6.13@umi/bin/umi.js" "$@"
  ret=$?
fi
exit $ret
