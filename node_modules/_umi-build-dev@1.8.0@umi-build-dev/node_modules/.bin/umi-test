#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_umi-test@1.5.3@umi-test/bin/umi-test.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_umi-test@1.5.3@umi-test/bin/umi-test.js" "$@"
  ret=$?
fi
exit $ret
